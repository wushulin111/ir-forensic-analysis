# ir-forensic-analysis 使用说明 v5.2.0

> 应急响应取证分析 Skill v5.2.0 — 静态取证包深度分析，覆盖 **11 大检测维度**（含打包器/多阶段载荷识别、勒索家族分类、供应链/第三方运维、银狐专项+远控对抗），支持因果链推理攻击路径复盘 + **6 源威胁情报自动关联**（微步/OTX/URLhaus/VT/CVERC/AbuseIPDB）。
>
> **配套采集工具**：Windows `IR_Collect_v5.1.ps1`（稳定版）/ `IR_Collect_v5.2.ps1`（定向增强版） | Linux `who.sh` | macOS `mac_collect.sh`

---

## 1. 快速开始

### 方式一：AI 对话分析（推荐）

直接把取证包丢给 AI：

```
帮我分析这个 IR.zip
分析一下这个取证包
看看这台服务器是不是被入侵了
```

AI 自动解压 → 数据预处理（timeline + Sigma + 情报查询）→ 11 维逐维分析 → 输出结构化报告。

### 方式二：Python 脚本命令行分析

```bash
# 1. 解压
python scripts/extract_archive.py IR.zip -o ./extracted

# 2. 分析
python scripts/analyze_forensics.py ./extracted/IR_HOSTNAME_TIMESTAMP/ -r ./rules -o ./report

# 3. 查看报告
cat ./report/incident_report.md
```

> 脚本为参考实现。AI 对话式分析不需要手动调用脚本。

---

## 2. 采集取证包

### Windows 脚本选型

现场先根据场景选择脚本：

| 脚本 | 定位 | 什么时候用 |
|------|------|-----------|
| `IR_Collect_v5.1.ps1` | 标准全量采集，9 大阶段 | 常规应急响应、疑似入侵、需要完整主机证据时默认使用 |
| `IR_Collect_v5.2.ps1` | 全量采集 + `-Target` 定向增强 | 遇到 v5.1 未覆盖的特定软件/组件，如 VPN、远控、Tailscale、安全软件等，需要补充定向取证时使用 |

### Windows 标准版 — `IR_Collect_v5.1.ps1`

```powershell
# 管理员 PowerShell 运行（无附加参数）
powershell.exe -ExecutionPolicy Bypass -File IR_Collect_v5.1.ps1
```

v5.1 为全量稳定版，启动后自动请求管理员权限，无附加参数。进度窗口显示采集阶段，可随时取消。

**采集内容（9 个阶段）**：

| 阶段 | 采集模块 | 输出目录 |
|------|---------|---------|
| 1/9 | 网络连接 / 进程列表 / 签名验证 / 进程名异常 / 打包器检测 / 远控检测 | `1_volatile/` |
| 2/9 | net user / WMI 账号 / ProfileList / SAM 克隆检测 / 多源交叉比对 | `2_accounts/` |
| 3/9 | 注册表自启动 / 计划任务 / 服务 / WMI / COM/DLL 劫持 / IFEO / Winlogon | `3_persistence/` |
| 4/9 | VSS 卷影副本 / 勒索扩展名扫描 / 勒索信检测 / 批量加密时间线 | `5_filesystem/ransomware_vss/` |
| 5/9 | 系统信息 / OS / 补丁 / 磁盘 / 环境变量 / 驱动 | `6_logs/` |
| 6/9 | 文件系统 / TEMP 目录 / 启动文件夹 / Prefetch / 静态EXE扫描 | `5_filesystem/` |
| 7/9 | 事件日志 (System/Security/Application/PowerShell) | `6_logs/` |
| 8/9 | IIS 配置 / Web 日志 / **浏览器访问记录与下载记录** | `7_web/` + `browser_artifacts/` |
| 9/9 | 延迟网络重采 (30s) / TEMP_MEI 对比 / 哈希链 / 压缩打包 | `12_Metadata/` |

**v4.3 新增采集项**：
- **WMI `Win32_UserAccount` 枚举**：独立代码路径，绕过 NetUserEnum API Hook
- **ProfileList 注册表枚举**：所有登录过用户的配置文件记录
- **多源交叉比对**：net user / WMI / ProfileList 三方数据差异自动告警
- **打包器检测**：PyInstaller / PyArmor / UPX / Enigma 进程识别
- **延迟网络二次采样**：采集后 30 秒再次 netstat，检测延时 C2 外连
- **浏览器取证**：Chrome / Edge / Firefox 历史记录和下载记录
- **Cancel 按钮**：进度窗口可随时安全中止采集

### Windows 定向增强版 — `IR_Collect_v5.2.ps1`

v5.2 包含 v5.1 的全部能力，额外支持**按需定向采集**。不传 `-Target` 时等价于 v5.1；传入 `-Target 名称` 时，会在全量采集基础上追加目标定向采集，不需要改主脚本。

```powershell
# 针对 Tailscale 定向采集（进程/服务/安装软件/注册表/文件）
powershell.exe -ExecutionPolicy Bypass -File IR_Collect_v5.2.ps1 -Target tailscale

# 不带规则文件时走通用兜底：按关键词扫描进程、服务、安装软件和文件
powershell.exe -ExecutionPolicy Bypass -File IR_Collect_v5.2.ps1 -Target anyapp
```

定向采集结果输出到压缩包内的 `9_extra/<目标名>/`，包含：

- `processes.csv`：匹配进程的 PID / 路径 / 命令行 / SHA256
- `services.csv`、`installed.csv`：匹配服务与已安装软件
- `registry_*.txt`：规则中指定的注册表键导出
- `files.csv` + `files/`：匹配文件清单；**敏感文件（`*.state`、`*.key`、`*.pem`、`*.pfx`、`*.conf`、`*.config`、`*.json`、`*.db`、`*.sqlite*`、`*.log`、`*.bak`）只记录 SHA256/元数据，不复制原始内容**；可执行/脚本类文件（exe/dll/sys/msi/bat/cmd/ps1/vbs/js/jar/py）才复制到 `files/`
- `summary.txt`：本次定向采集统计

规则文件放在脚本同级 `targets/` 目录（脚本位于 `assets/` 下时，自动回退读取 skill 根目录 `config/targets/`），文件名与目标名一致，如 `tailscale.json`：

```json
{
  "keyword": "tailscale",
  "process_names": ["tailscale", "tailscaled"],
  "service_names": ["Tailscale"],
  "registry_keys": ["HKLM\\SOFTWARE\\Tailscale IPN"],
  "search_roots": ["%ProgramFiles%\\Tailscale", "%LOCALAPPDATA%\\Tailscale"],
  "search_depth": 3,
  "max_files": 200
}
```

字段说明：`keyword` 为通用匹配关键词；`process_names` / `service_names` 精确匹配进程与服务名；`registry_keys` 需要导出的注册表键；`search_roots` 文件搜索根目录（支持 `%ProgramFiles%` 等环境变量）；`search_depth` 默认 4 层；`max_files` 默认最多记录 200 个文件。仓库已附带 `config/targets/tailscale.json` 示例，可直接照抄新增其他目标。

### Linux — `who.sh`（司稽）

```bash
# root 权限运行
sudo ./who.sh -q    # 全量取证
sudo ./who.sh -f    # 快速模式
```

### macOS — `mac_collect.sh`（v2.1）

macOS 取证采集脚本 v2.1，兼容 macOS 10.15+（Catalina ~ Sequoia），Intel / Apple Silicon 双架构，完全离线运行。

```bash
chmod +x mac_collect.sh
sudo ./mac_collect.sh
```

采集内容：系统信息、网络与监听、外联 IP（IPv4/IPv6）、进程与签名验证、LaunchDaemons/LaunchAgents、Cron/at/sudoers、账号与隐藏用户、SSH 密钥、浏览器历史（Safari/Chrome/Edge/Firefox）、下载隔离记录、系统日志、安全配置（SIP/Gatekeeper/FileVault/MDM/TCC）、内核扩展、关键清单摘要与包 SHA256。

输出：`/opt/ir_evidence/IR_{主机名}_{时间戳}/` 原始数据、`/opt/ir_evidence/IR_{主机名}_{时间戳}.tar.gz` 打包、同目录 `.sha256` 包哈希。

---

## 3. 11 大检测维度

| # | 维度 | 检测内容 | ATT&CK |
|---|------|----------|--------|
| 1 | 可疑进程 | 异常路径/隐藏目录/已知恶意进程名/LotL滥用/进程注入指标/勒索加密进程 | T1059, T1218, T1564 |
| 2 | 持久化机制 | 注册表Run键/计划任务/WMI事件订阅/启动文件夹/服务劫持/IFEO | T1547, T1053, T1546, T1543 |
| 3 | 异常账号 | **`$`后缀隐藏/WMI交叉验证/ProfileList交叉验证/SAM克隆账号(F值对比)** | T1136, T1078, T1548 |
| 4 | 网络外联 | C2端口/DNS异常/CLOSE_WAIT累积/反向Shell/**延迟二次采样对比** | T1071, T1571, T1041 |
| 5 | Webshell | 一句话木马/大马/内存马/杀软对抗/常见文件名 | T1505, T1059, T1562 |
| 6 | 内存注入 | RWX区域/反射DLL/进程空心化/Beacon特征/**浏览器JIT误报过滤** | T1055, T1620 |
| 7 | 横向移动 | SMB/RDP/SSH扫描/PsExec/WMI远程/哈希传递/WinRM | T1021, T1047, T1550 |
| 8 | 高级对抗 | 银狐专项(34条规则/5265域名/393IP)+远控对抗：Unhooking/自编写驱动/白加黑 | T1562, T1055, T1014 |
| 9 | 进程名仿冒+签名 | 字符替换仿冒(svch0st→svchost)/Levenshtein偏差/数字签名验证 | T1036.005, T1036.002 |
| 10 | **打包器/载荷** | **PyInstaller/PyArmor/UPX/Enigma检测/TEMP_MEI快照对比/多阶段加载** | T1027, T1059 |
| 11 | **供应链/勒索** | **勒索家族分类(8族)+ESXi/NAS专项+第三方运维盲区+因果链攻击路径复盘** | T1486, T1490, T1195 |

> 第 10 维（v4.2 新增）和第 11 维（v3.0 新增，原为第 9/10 维）为后续版本扩展维度。

### 分析流程

```
收到取证包
├─ 数据预处理
│   ├─ 解压 → 统计文件数量和类型
│   ├─ 事件归一化 → timeline.jsonl（进程+网络+文件+注册表统一事件流）
│   ├─ Sigma 规则匹配 → 标记可疑命令行
│   ├─ 威胁情报查询 → 外联 IP/域名/Hash 关联情报
│   └─ 实体关联图构建（进程←→网络←→文件）
├─ 快速评估 → 扫描 P0/P1 关键文件，判定威胁等级
├─ 11 维逐维深度分析（每个维度交叉验证）
├─ 因果链推理（逆向溯源 ≥ 3 层）
├─ 计算威胁评分 + Dwell Time 评估
└─ 输出报告（Markdown + JSON + IOC 摘要）
```

---

## 4. 威胁情报体系

### 情报源总览（6 源）

| 情报源 | 类型 | 查询对象 | 是否免费 | 状态 |
|--------|------|---------|---------|------|
| **微步在线** | 商业 API | IP / 域名 / Hash | 需 Key | ✅ 已配置 |
| **AlienVault OTX** | 社区 API | IP / 域名 / Hash（Pulse关联+恶意家族） | 免费 | ✅ 可用 |
| **URLhaus** | 开源 API | IP / 域名（历史分发恶意URL记录） | 免费 | ✅ 可用 |
| **VirusTotal** | 商业 API | IP / Hash | 需 Key | ⏳ Key为空时跳过 |
| **CVERC** | 国家平台 | Hash | 需 Key | ⏳ Key为空时跳过 |
| **AbuseIPDB** | 社区 API | IP（信誉评分 0-100） | 免费 1000次/天 | ⏳ Key为空时跳过 |
| **IPinfo.io** | 免费 API | IP 归属地/ASN | 免费 | ✅ 可用 |

### 查询优先级

```
IP: 微步 → OTX → URLhaus → VT → AbuseIPDB → IPinfo
域名: 微步 + OTX（并行）
Hash: 微步 → OTX → VT → CVERC
```

### 配置情报源

复制 `config/threat_intel.json.example` 为 `config/threat_intel.json`，然后填入对应源的 `api_key`：

```json
{
  "sources": [
    { "name": "VirusTotal", "api_key": "你的VT_KEY" },
    { "name": "CVERC国家计算机病毒协同分析平台", "api_key": "你的CVERC_KEY" },
    { "name": "AbuseIPDB", "api_key": "你的ABUSEIPDB_KEY" }
  ]
}
```

Key 为空时该源自动跳过，不影响主流程。

### 测试

```bash
python scripts/threat_intel_lookup.py
```

---

## 5. 威胁评分体系

| 评分区间 | 等级 | 图标 | 处置要求 |
|---------|------|------|---------|
| 0-20 | 低危 | 🟢 | 观察记录 |
| 21-50 | 中危 | 🟡 | 24h 内排查 |
| 51-80 | 高危 | 🟠 | 4h 内响应 |
| 81+ | 危急 | 🔴 | 15min 内响应，通知 CISO |

**计分规则**：恶意进程 +15/个 | 恶意外联 +10/个 | 恶意计划任务 +12/个 | RWX内存 +10/个 | 隐藏账户 +20/个 | 日志清除 +15 | VSS删除 +20 | 威胁情报命中 +5/个

---

## 6. 应急响应流程

| 等级 | 事件类型 | 响应时限 | 典型动作 |
|------|---------|---------|---------|
| P0 | 活跃勒索 / 数据泄露 / C2 活跃 | 15min | 断网隔离 → 保全内存镜像 → 通知 CISO |
| P1 | 发现后门 / Webshell / 凭据窃取 | 4h | 清除持久化 → 重置密码 → 阻断外联 |
| P2 | 可疑进程 / 异常计划任务 / 可疑账号 | 24h | 取证分析 → 确认范围 → 按需处置 |
| P3 | 低危告警 / 配置不当 / 信息泄露 | 1周 | 记录在案 → 纳入加固计划 |

---

## 7. 目录结构

```
ir-forensic-analysis/
├── SKILL.md                       # 核心分析 Skill（LLM 使用）
├── 使用说明.md                    # 本文档
├── CHANGELOG.md                   # 完整变更记录
├── assets/                        # 采集脚本与模板
│   ├── IR_Collect_v5.1.ps1       # ★ Windows 采集脚本稳定版（1010行，v4.3/v5.0合并版）
│   ├── IR_Collect_v5.2.ps1       # ★ Windows 定向增强版（新增 -Target 按需采集）
│   ├── who.sh                     # Linux 采集脚本（司稽 v8.1）
│   ├── mac_collect.sh             # macOS 采集脚本（v2.1）
│   ├── report_template.md         # 报告模板
├── config/
│   ├── threat_intel.json          # 威胁情报 API 配置
│   └── targets/                   # v5.2 定向采集规则（tailscale.json 等）
├── scripts/                       # 独立 Python 分析引擎
│   ├── analyze_forensics.py       # 取证包分析主引擎（46KB）
│   ├── extract_archive.py         # ZIP/tar.gz 解压
│   ├── rule_manager.py            # 规则管理
│   └── threat_intel_lookup.py     # 威胁情报查询（v2.1, 6源）
├── rules/                         # 检测规则库（JSON + YARA）
│   ├── account_rules.json         # 账号检测规则
│   ├── process_rules.json         # 进程检测规则
│   ├── persistence_rules.json     # 持久化检测规则
│   ├── injection_rules.json       # 内存注入规则（16条）
│   ├── lateral_movement_rules.json
│   ├── webshell_rules.json
│   ├── silver_fox_rules.json      # 银狐专项规则（34条）
│   ├── ransomware_family_rules.json
│   ├── ip_rules.json / ioc_library.json
│   └── yara_rules/                # YARA 规则（银狐/勒索/注入/Webshell/malware）
└── references/                    # 参考文档
    ├── ATTACK_CHAIN_REASONING.md  # 因果链推理方法论
    ├── DETECTION_RULES.md         # 检测规则详细说明（34KB）
    ├── DIRECTORY_MAPPING.md       # 取证包目录映射
    ├── LOG_PATH_REFERENCE.md      # 操作系统日志路径参考
    ├── REPORT_CONTRACT.md         # 报告格式契约
    ├── RULES_FORMAT.md            # 规则格式规范
    └── SCRIPTS_GUIDE.md           # 脚本使用指南
```

---

## 8. 故障排查

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| `.ps1` 无法运行 | PowerShell 执行策略限制 | `powershell -ExecutionPolicy Bypass -File xxx.ps1` |
| 压缩包生成后无法找到路径 | 中文路径或权限问题 | 以管理员运行，确保 C:\IR 目录可写 |
| 中文乱码 | 编码问题 | PS 5.1 用 UTF-16 LE；Python 用 UTF-8 无 BOM |
| 解压失败 | 文件损坏 | 检查 md5，确认磁盘空间 |
| SAM 注册表读取失败 | 仅管理员权限（需 SYSTEM） | 脚本自动降级输出 SKIP 标记，不影响主流程 |
| 威胁情报无结果 | API Key 未配置 | 编辑 `config/threat_intel.json` 填入 Key |
| 大 CSV 加载过慢 | 文件过大 | 先 grep 定向搜索，再针对性读取匹配行 |

---

## 9. 版本历史摘要

| 版本 | 日期 | 主要变化 |
|------|------|---------|
| **v5.2.0** | 2026-08 | **定向采集**：-Target 按需采集 / config/targets/*.json 规则 / 通用兜底 / 敏感文件仅哈希 |
| **v4.3.0** | 2026-07 | OTX + URLhaus 情报集成 / **WMI+ProfileList 多源账号交叉比对** / 浏览器取证 |
| **v4.2.0** | 2026-07 | 打包器检测(PyInstaller/PyArmor/UPX) / 延迟网络二次采样 / Cancel按钮 |
| **v3.6.0** | 2026-06 | 威胁情报自动关联(微步) / 银狐规则扩至 34 条 / IOC 域名库 5265 条 |
| **v3.5.0** | 2026-05 | 内存注入规则 16 条 / RWX 误报分层过滤 / DETECTION_RULES 重写 |
| **v3.4.0** | 2026-05 | IR_Collect v4.0 完全离线重写 / 哈希链完整性 / macOS 支持 |
| **v3.2.0** | 2026-05 | 事件归一化(timeline.jsonl) / Sigma 规则匹配 / 实体关联图 |

完整变更记录见 `CHANGELOG.md`。
